configs:
  dotenv:
    file: .env

services:
  db:
    image: mariadb:latest@sha256:b3af559fc38e013c50dd5b5993bfbbb21125c1a62d976b3b0a14d9c4acaae578
    restart: always
    shm_size: 128mb
    environment:
      MARIADB_USER: ${MARIADB_USER}
      MARIADB_PASSWORD : ${MARIADB_PASSWORD}
      MARIADB_ROOT_PASSWORD: ${MARIADB_PASSWORD}
      MARIADB_DATABASE: ${MARIADB_DATABASE}
    volumes:
      - /var/lib/mysql:/var/lib/mysql
    ports:
      - 3306:3306
      
  api:
    build: ./api
    image: focus-marches-v2-api
    ports:
      - 8000:80
    environment:
      DATABASE_URL: mariadb+mariadbconnector://${MARIADB_USER}:${MARIADB_PASSWORD}@db:3306/${MARIADB_DATABASE}
    configs:
      - source: dotenv
        target: /.env

  front:
    build: ./front
    image: focus-marches-v2-front
    ports:
      - 80:80
    env_file: ".env"